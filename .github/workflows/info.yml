# crée un fichier avec le contenu de la variable d'environnement APIS_KEYS en ".env" sur vscode avec la commande echo $APIS_KEYS > .env

name: 'APIs'

on:
  push:
    branches: ["main"]
  workflow_dispatch:


permissions: read-all

# Environnement local pour les secrets.


env:
  APIS_KEYS: ${{ secrets.APIS_KEYS }}

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: create .env file
        shell: pwsh
        run: echo "api_keys = ${{secrets.APIS_KEYS}}" > /.env


