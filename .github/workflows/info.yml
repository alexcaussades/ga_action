name: info

on: 
    workflow_dispatch:
    


jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Creat .env file
        shell: pwsh
        run: |
          echo "Creating .env file"
          echo file".env"
          new-item -path ${{env}} -name .env -value "APP_API_KEY = ${{secrets.APIS_KEYS}}"
      - name: Deploy
        run: echo "Deploying!"